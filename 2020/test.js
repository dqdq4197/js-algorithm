let N = 4; // N명, 마을도 N개
let M = 8; // 간선, 도로의 갯수
let X = 2; // 목적지, 2번 마을에서 파티시작
let graph = [
  [], 
  [[2,4], [3,2], [4,7]],
  [[1,1], [3,5]],
  [[1,2], [4,4]], 
  [[2, 3]]
]
let goPartyDis = [];
let d = new Array(N + 1).fill(0);

function goParty() {
  for(let i = 1; i <= N; i++) {

  }
}
